# topic-effects.ts 生成说明

本程序消费 `ExportedRogue6TopicExtReport` 中三类黑流树海主题效果，并严格限制在当前 FormulaBook 已有属性乘区内：

- 实托邦“黑流地脉”按原文百分比写入敌方直接攻击力和最大生命倍率。
- 实托邦“希望的沃土”按原文百分比写入我方局内攻击力和最大生命倍率。
- 乌托邦“孤立石林”由编排层派生为两个互斥状态，分别写入直接攻击速度 `+30` 和 `-50`。
- 概念体“白模鸟”“涂装黎博利”在用户选中后视为触发条件满足，分别写入直接攻击速度 `+50` 和 `+35`。

“巨人摇篮”等缺少明确数值的效果、缺少运行时条件的效果或 FormulaBook 尚无对应乘区的效果保持 `unknown`；资源、地图、控制和视觉效果标记为 `not_applicable`。原文格式变化时数值解析失败并回退为 `unknown`，避免静默沿用旧版本常量。

本说明由 AI 生成，可删除或重新生成，不属于机制证据。
