# registry.ts 生成说明

当前 `ExportedRogueStageReportItem` 保留完整 Level JSON，但尚未把 Level、替换 Level、敌人、装置和关卡 buff 整理成 mechanics 可稳定消费的字段。本注册表暂为空，所有关卡分析返回 `unknown`。

未来应按 Level 中重复出现的机制签名复用程序，关卡 ID 只作为配置事实，不得为每个关卡生成一个函数。

本说明由 AI 生成，可删除或重新生成，不属于机制证据。
