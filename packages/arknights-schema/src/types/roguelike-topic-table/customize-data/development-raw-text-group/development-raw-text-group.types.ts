/**

 * JSON 逻辑模块 `customize-data/development-raw-text-group/development-raw-text-group` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 CustomizeDatumDevelopmentRawTextGroup 的显式 TypeScript 定义。
 *
 * 字段语义应直接维护在类型与字段的源码注释中。
 */
export interface CustomizeDatumDevelopmentRawTextGroup {
  /** 原始数据字段 `nodeIdList`。 */
  nodeIdList: string[];
  /** 原始数据字段 `useLevelMark`。 */
  useLevelMark: boolean;
  /** 原始数据字段 `groupIconId`。 */
  groupIconId: string;
  /** 原始数据字段 `useUpBreak`。 */
  useUpBreak?: boolean;
  /** 原始数据字段 `sortId`。 */
  sortId: number;
}
