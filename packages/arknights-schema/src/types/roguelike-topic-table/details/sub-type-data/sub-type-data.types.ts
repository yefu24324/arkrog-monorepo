/**

 * JSON 逻辑模块 `details/sub-type-data/sub-type-data` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 DetailSubTypeDatum 的显式 TypeScript 定义。
 *
 * 字段语义应直接维护在类型与字段的源码注释中。
 */
export interface DetailSubTypeDatum {
  /** 原始数据字段 `eventType`。 */
  eventType: string;
  /** 原始数据字段 `subTypeId`。 */
  subTypeId: number;
  /** 原始数据字段 `iconId`。 */
  iconId: null | string;
  /** 原始数据字段 `name`。 */
  name: string;
  /** 原始数据字段 `description`。 */
  description: null | string;
}
