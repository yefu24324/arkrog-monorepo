/**

 * JSON 逻辑模块 `details/fail-endings/fail-endings` 的显式类型。

 */

import type { Ro4FailEnding1 } from "../../shared/rogue-variants.types.js";

/**
 * roguelike_topic_table.json 中 FailEndings 的显式 TypeScript 定义。
 *
 * 字段语义可在对应中文 Markdown 文档的人工说明区继续补充。
 */
export interface FailEndings {
  /** 原始数据字段 `ro4_fail_ending_1`。 */
  ro4_fail_ending_1?: Ro4FailEnding1;
}
