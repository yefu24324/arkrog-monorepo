/**

 * JSON 逻辑模块 `details/node-type-data/node-type-data` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 NodeTypeDatum 的显式 TypeScript 定义。
 *
 * 字段语义应直接维护在类型与字段的源码注释中。
 */
export interface NodeTypeDatum {
  /** 原始数据字段 `name`。 */
  name: string;
  /** 原始数据字段 `subName`。 */
  subName: null | string;
  /** 原始数据字段 `description`。 */
  description: string;
}
