/**

 * JSON 逻辑模块 `details/style-config/style-config` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 StyleConfig 的显式 TypeScript 定义。
 *
 * 字段语义可在对应中文 Markdown 文档的人工说明区继续补充。
 */
export interface StyleConfig {
  /** 原始数据字段 `expStyleConfig`。 */
  expStyleConfig: null;
}
