/**

 * JSON 逻辑模块 `details/milestone-updates/milestone-updates` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 MilestoneUpdate 的显式 TypeScript 定义。
 *
 * 字段语义可在对应中文 Markdown 文档的人工说明区继续补充。
 */
export interface MilestoneUpdate {
  /** 原始数据字段 `updateTime`。 */
  updateTime: number;
  /** 原始数据字段 `endTime`。 */
  endTime: number;
  /** 原始数据字段 `maxBpLevel`。 */
  maxBpLevel: number;
  /** 原始数据字段 `maxBpCount`。 */
  maxBpCount: number;
  /** 原始数据字段 `maxDisplayBpCount`。 */
  maxDisplayBpCount: number;
}
