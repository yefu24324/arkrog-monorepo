/**

 * JSON 逻辑模块 `details/band-ref/band-ref` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 BandRef 的显式 TypeScript 定义。
 *
 * 字段语义可在对应中文 Markdown 文档的人工说明区继续补充。
 */
export interface BandRef {
  /** 原始数据字段 `itemId`。 */
  itemId: string;
  /** 原始数据字段 `bandLevel`。 */
  bandLevel: number;
  /** 原始数据字段 `normalBandId`。 */
  normalBandId: string;
}
