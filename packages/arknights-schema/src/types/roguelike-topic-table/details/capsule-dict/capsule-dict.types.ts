/**

 * JSON 逻辑模块 `details/capsule-dict/capsule-dict` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 CapsuleDict 的显式 TypeScript 定义。
 *
 * 字段语义可在对应中文 Markdown 文档的人工说明区继续补充。
 */
export interface CapsuleDict {
  /** 原始数据字段 `itemId`。 */
  itemId: string;
  /** 原始数据字段 `maskType`。 */
  maskType: string;
  /** 原始数据字段 `innerColor`。 */
  innerColor: string;
}
