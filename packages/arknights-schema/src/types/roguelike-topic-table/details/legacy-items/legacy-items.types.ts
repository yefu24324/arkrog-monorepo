/**

 * JSON 逻辑模块 `details/legacy-items/legacy-items` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 LegacyItem 的显式 TypeScript 定义。
 *
 * 字段语义可在对应中文 Markdown 文档的人工说明区继续补充。
 */
export interface LegacyItem {
  /** 原始数据字段 `legacyId`。 */
  legacyId: string;
  /** 原始数据字段 `hideLegacyItem`。 */
  hideLegacyItem: boolean;
  /** 原始数据字段 `legacyGroupId`。 */
  legacyGroupId: null | string;
}
