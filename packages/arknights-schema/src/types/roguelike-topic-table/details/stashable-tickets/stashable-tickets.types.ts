/**

 * JSON 逻辑模块 `details/stashable-tickets/stashable-tickets` 的显式类型。

 */

/**
 * roguelike_topic_table.json 中 StashableTicket 的显式 TypeScript 定义。
 *
 * 字段语义应直接维护在类型与字段的源码注释中。
 */
export interface StashableTicket {
  /** 原始数据字段 `ticketId`。 */
  ticketId: string;
  /** 原始数据字段 `stashedTicketId`。 */
  stashedTicketId: string;
}
