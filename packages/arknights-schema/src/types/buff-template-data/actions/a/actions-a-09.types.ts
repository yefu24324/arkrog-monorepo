/**
 * buff_template_data 的 A 组 Action 显式类型。
 */

import type { BattleAction } from "../../battle-action.types.js";

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49SideCheckCharacterTileType, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49SideCheckCharacterTileTypeAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49SideCheckCharacterTileType, Assembly-CSharp";
  /** Act49SideCheckCharacterTileTypeAction 的 `checkAnyTile` 配置字段；准确战斗语义待确认。 */
  "_checkAnyTile": boolean;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
  /** Act49SideCheckCharacterTileTypeAction 的 `tileType` 配置字段；准确战斗语义待确认。 */
  "_tileType": "Carp" | "Empty" | "None" | "Pure";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49SideCheckWordTileBuildable, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49SideCheckWordTileBuildableAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49SideCheckWordTileBuildable, Assembly-CSharp";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49SideDestroyCarpTile, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49SideDestroyCarpTileAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49SideDestroyCarpTile, Assembly-CSharp";
  /** Act49SideDestroyCarpTileAction 的 `targetTileType` 配置字段；准确战斗语义待确认。 */
  "_targetTileType": "Empty";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
  /** Act49SideDestroyCarpTileAction 的 `tileType` 配置字段；准确战斗语义待确认。 */
  "_tileType": "Carp";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideEnemySheartCheckTriggerSkill, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideEnemySheartCheckTriggerSkillAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideEnemySheartCheckTriggerSkill, Assembly-CSharp";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideEnemySsttzFinishSkillLockEffect, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideEnemySsttzFinishSkillLockEffectAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideEnemySsttzFinishSkillLockEffect, Assembly-CSharp";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideEnemySsttzShowSkillLockEffect, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideEnemySsttzShowSkillLockEffectAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideEnemySsttzShowSkillLockEffect, Assembly-CSharp";
  /** Act49sideEnemySsttzShowSkillLockEffectAction 的 `posKey` 配置字段；准确战斗语义待确认。 */
  "_posKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49SideEnemyTjglyLockSelfWithTile, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49SideEnemyTjglyLockSelfWithTileAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49SideEnemyTjglyLockSelfWithTile, Assembly-CSharp";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49SideEnemyTjglyTryFindNextTile, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49SideEnemyTjglyTryFindNextTileAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49SideEnemyTjglyTryFindNextTile, Assembly-CSharp";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49SideExtractCharacter, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49SideExtractCharacterAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49SideExtractCharacter, Assembly-CSharp";
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideHeadBossSetParams, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideHeadBossSetParamsAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideHeadBossSetParams, Assembly-CSharp";
  /** Act49sideHeadBossSetParamsAction 的 `doomCountDownKey` 配置字段；准确战斗语义待确认。 */
  "_doomCountDownKey": string;
  /** Act49sideHeadBossSetParamsAction 的 `dotAttackAtkScaleKey` 配置字段；准确战斗语义待确认。 */
  "_dotAttackAtkScaleKey": string;
  /** Act49sideHeadBossSetParamsAction 的 `dotAttackDurationKey` 配置字段；准确战斗语义待确认。 */
  "_dotAttackDurationKey": string;
  /** Act49sideHeadBossSetParamsAction 的 `dotAttackEleAtkScaleKey` 配置字段；准确战斗语义待确认。 */
  "_dotAttackEleAtkScaleKey": string;
  /** Act49sideHeadBossSetParamsAction 的 `dotAttackTriggerIntervalKey` 配置字段；准确战斗语义待确认。 */
  "_dotAttackTriggerIntervalKey": string;
  /** Act49sideHeadBossSetParamsAction 的 `mainAttackAtkScaleKey` 配置字段；准确战斗语义待确认。 */
  "_mainAttackAtkScaleKey": string;
  /** Act49sideHeadBossSetParamsAction 的 `mainAttackEleAtkScaleKey` 配置字段；准确战斗语义待确认。 */
  "_mainAttackEleAtkScaleKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/** 本模块 Action 分支的局部联合，用于分层构造完整递归联合。 */
export type BattleActionA09 = Act49SideCheckCharacterTileTypeAction | Act49SideCheckWordTileBuildableAction | Act49SideDestroyCarpTileAction | Act49sideEnemySheartCheckTriggerSkillAction | Act49sideEnemySsttzFinishSkillLockEffectAction | Act49sideEnemySsttzShowSkillLockEffectAction | Act49SideEnemyTjglyLockSelfWithTileAction | Act49SideEnemyTjglyTryFindNextTileAction | Act49SideExtractCharacterAction | Act49sideHeadBossSetParamsAction;
