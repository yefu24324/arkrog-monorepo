/**
 * buff_template_data 的 A 组 Action 显式类型。
 */

import type { BattleAction } from "../../battle-action.types.js";

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossLevelSummonHolderTrap, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossLevelSummonHolderTrapAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossLevelSummonHolderTrap, Assembly-CSharp";
  /** Act49sideBossLevelSummonHolderTrapAction 的 `partName` 配置字段；准确战斗语义待确认。 */
  "_partName": string;
  /** Act49sideBossLevelSummonHolderTrapAction 的 `partNameKey` 配置字段；准确战斗语义待确认。 */
  "_partNameKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossLevelTriggerLevelFinish, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossLevelTriggerLevelFinishAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossLevelTriggerLevelFinish, Assembly-CSharp";
  /** Act49sideBossLevelTriggerLevelFinishAction 的 `delayToFinishLevel` 配置字段；准确战斗语义待确认。 */
  "_delayToFinishLevel": number;
  /** Act49sideBossLevelTriggerLevelFinishAction 的 `delayToKillAll` 配置字段；准确战斗语义待确认。 */
  "_delayToKillAll": number;
  /** Action 读取的来源实体类型。 */
  "_sourceType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossRegisterDefaultSkillFocusGrid, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossRegisterDefaultSkillFocusGridAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossRegisterDefaultSkillFocusGrid, Assembly-CSharp";
  /** Act49sideBossRegisterDefaultSkillFocusGridAction 的 `defaultPosKey` 配置字段；准确战斗语义待确认。 */
  "_defaultPosKey": string;
  /** Act49sideBossRegisterDefaultSkillFocusGridAction 的 `partName` 配置字段；准确战斗语义待确认。 */
  "_partName": string;
  /** Act49sideBossRegisterDefaultSkillFocusGridAction 的 `partNameKey` 配置字段；准确战斗语义待确认。 */
  "_partNameKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossRegistSelf, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossRegistSelfAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossRegistSelf, Assembly-CSharp";
  /** Act49sideBossRegistSelfAction 的 `partName` 配置字段；准确战斗语义待确认。 */
  "_partName": string;
  /** Act49sideBossRegistSelfAction 的 `partNameKey` 配置字段；准确战斗语义待确认。 */
  "_partNameKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossSyncHPRatio, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossSyncHPRatioAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossSyncHPRatio, Assembly-CSharp";
  /** Action 读取的来源实体类型。 */
  "_sourceType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossTailChargeSummonCnt, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossTailChargeSummonCntAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossTailChargeSummonCnt, Assembly-CSharp";
  /** Act49sideBossTailChargeSummonCntAction 的 `cnt` 配置字段；准确战斗语义待确认。 */
  "_cnt": number;
  /** Act49sideBossTailChargeSummonCntAction 的 `scaleKey` 配置字段；准确战斗语义待确认。 */
  "_scaleKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossTailSetSummonEnemyInterval, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossTailSetSummonEnemyIntervalAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossTailSetSummonEnemyInterval, Assembly-CSharp";
  /** Act49sideBossTailSetSummonEnemyIntervalAction 的 `interval` 配置字段；准确战斗语义待确认。 */
  "_interval": number;
  /** Act49sideBossTailSetSummonEnemyIntervalAction 的 `intervalKey` 配置字段；准确战斗语义待确认。 */
  "_intervalKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossUpdateSkillFocusGrid, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossUpdateSkillFocusGridAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossUpdateSkillFocusGrid, Assembly-CSharp";
  /** Act49sideBossUpdateSkillFocusGridAction 的 `partName` 配置字段；准确战斗语义待确认。 */
  "_partName": string;
  /** Act49sideBossUpdateSkillFocusGridAction 的 `partNameKey` 配置字段；准确战斗语义待确认。 */
  "_partNameKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideBossUpdateWarningEffect, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideBossUpdateWarningEffectAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideBossUpdateWarningEffect, Assembly-CSharp";
  /** Act49sideBossUpdateWarningEffectAction 的 `force` 配置字段；准确战斗语义待确认。 */
  "_force": boolean;
  /** Act49sideBossUpdateWarningEffectAction 的 `partName` 配置字段；准确战斗语义待确认。 */
  "_partName": string;
  /** Act49sideBossUpdateWarningEffectAction 的 `partNameKey` 配置字段；准确战斗语义待确认。 */
  "_partNameKey": string;
  /** Action 作用的目标实体类型。 */
  "_targetType": "BUFF_OWNER";
}

/**
 * CLR 组件 `Torappu.Battle.Action.Nodes+Act49sideChargePrintingProgress, Assembly-CSharp` 的严格 Action 数据。
 *
 * 字段来自当前完整真实数据；带“待确认”的语义需要结合客户端实现继续核实。
 */
export interface Act49sideChargePrintingProgressAction {
  /** CLR Action 组件的完整类型名，也是递归动作联合的判别字段。 */
  "$type": "Torappu.Battle.Action.Nodes+Act49sideChargePrintingProgress, Assembly-CSharp";
  /** Act49sideChargePrintingProgressAction 的 `chargeValue` 配置字段；准确战斗语义待确认。 */
  "_chargeValue": number;
}

/** 本模块 Action 分支的局部联合，用于分层构造完整递归联合。 */
export type BattleActionA08 = Act49sideBossLevelSummonHolderTrapAction | Act49sideBossLevelTriggerLevelFinishAction | Act49sideBossRegisterDefaultSkillFocusGridAction | Act49sideBossRegistSelfAction | Act49sideBossSyncHPRatioAction | Act49sideBossTailChargeSummonCntAction | Act49sideBossTailSetSummonEnemyIntervalAction | Act49sideBossUpdateSkillFocusGridAction | Act49sideBossUpdateWarningEffectAction | Act49sideChargePrintingProgressAction;
